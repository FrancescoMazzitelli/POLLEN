#!/bin/sh
# POST /register not applicable for standard OpenAPI mocks.
# Services are registered via api-importer during deploy.sh.
echo "| Registration handled by deploy.sh via api-importer. Skipping."
